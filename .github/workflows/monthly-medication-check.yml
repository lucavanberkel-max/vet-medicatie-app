name: Maandelijkse Medicatie Check

on:
  schedule:
    - cron: '0 9 1 * *'  # Elke 1e van de maand om 9:00 UTC
  workflow_dispatch:      # Handmatig te starten via GitHub UI

jobs:
  check-medications:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Controleer nieuwe medicaties
        run: python scripts/check_new_medications.py
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          # EMA UPD (optioneel) — voeg toe zodra je geregistreerd bent:
          # EMA_CLIENT_ID:     ${{ secrets.EMA_CLIENT_ID }}
          # EMA_CLIENT_SECRET: ${{ secrets.EMA_CLIENT_SECRET }}
          # EMA_TENANT_ID:     ${{ secrets.EMA_TENANT_ID }}
